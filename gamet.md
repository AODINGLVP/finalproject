# 项目研究重点

## 核心研究目标

本项目研究的重点是：当前实现的一系列大型行为树显示优化方案，能否让游戏开发者更方便地使用可视化行为树开发和调试游戏人工智能。

项目首先实现一个能够实际驱动游戏敌人的完整 Godot 4.6 行为树插件，使实验对象不是只能展示节点的静态原型。复杂演示游戏中的敌人由 241 节点行为树控制，覆盖巡逻、索敌、追击、近战、远程攻击、跳跃、攀爬、越障、搜索、撤退和治疗等行为。这个游戏用于证明显示优化面对的是具有真实开发意义的行为树。

## 研究对象

实验比较优化前的 Baseline 与优化后的多种显示方式，包括：

- Compact Cards；
- Semantic Zoom 与 Optimized Overview；
- Fisheye 焦点与上下文；
- Subtree Collapse 与 Subtree Focus；
- Search 定位与非目标淡化；
- Minimap、Fit-to-view、Breadcrumb 与多列布局；
- Active Path、Branch Dimming 和失败原因等运行时检查显示。

每种优化必须可以独立开启和关闭，并能安全恢复到基线显示。比较过程中不得改变行为树资源结构、节点保存位置、父子关系和同级从左到右的执行顺序。

## 实验变量

实验采用三个主要自变量：

1. **显示条件**：优化前的 Baseline 与一种或多种优化后的显示条件。
2. **行为树节点数量**：使用 31、61、121、241 和 364 节点的确定性行为树，并用可玩的 241 节点复杂行为树进行补充验证。
3. **物理屏幕尺寸**：在当前连接的三块不同物理尺寸屏幕上重复实验。屏幕尺寸是研究因素；分辨率、刷新率、系统缩放和连接方式只作为审计信息，不作为主要解释变量。

核心比较关系为：

> 屏幕尺寸 × 节点数量 × 显示条件（优化前/优化后）

## 评判方式

项目通过同一行为树、同一任务和同一实验流程下的前后配对数据评价显示优化。主要记录：

- 画布内可见节点数量和比例；
- Fit-to-view 后的缩放比例与节点覆盖率；
- 节点卡片总面积和保留的信息字段；
- 节点重叠数量与最小父子间距；
- Search 淡化的非目标节点比例和目标是否进入视口；
- Focus、Collapse 后保留的上下文数量；
- Minimap 对整棵树的覆盖情况；
- 固定拖拽路径的处理时间、最终位置同步和执行顺序是否保持；
- 真实 GPU 截图中的文字裁切、连线、端口、缩放残留和焦点位置。

每个条件先预热，再进行多次重复测量。结果报告原始数据、中位数、四分位数、P95、前后差值或降低比例，并保留实验配置、截图和输入文件哈希，使后续接入新屏幕后能够追加实验而不覆盖旧数据。

## 论文中的结论边界

这些实验能够说明显示优化是否提高了界面信息密度、可见上下文、目标定位能力、复杂图的固定交互处理效率，以及是否在不同节点数量和物理屏幕尺寸下保持稳定。它们不能在没有真人参与者数据的情况下直接证明人的理解速度、错误率、认知负荷或长期开发效率得到提高。

因此论文应使用“改善了可测的显示与交互属性”“为更方便地使用大型行为树提供了客观支持”等表述，不能把面积降低或覆盖率提高直接写成同等比例的可读性提升。

## 不属于本论文重点的内容

行为树 Tick 成本、Actor 数量下的运行时扩展、CPU/GPU 资源消耗和拓扑缓存性能不作为本论文的研究问题、主要实验、核心贡献或结论。相关实现可以作为插件内部工程细节保留，但不参与本文对显示优化有效性的论证。
