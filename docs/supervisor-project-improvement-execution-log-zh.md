# 导师要求的七项项目改进：执行记录

**开始日期：** 2026-08-25  
**开始检查点：** `3445570`  
**范围：** 只处理 `supervisor-meeting-2026-08-25-action-checklist-zh.md` 中的七项项目工作，不修改论文正文。

## 停止边界

每项任务按以下规则执行：

1. 先建立可重复的失败或验收用例，再修改实现。
2. 同一个验收失败最多进行三次有实质差异的修复尝试。
3. 如果第三次仍不能达到验收要求，或者继续修改必须破坏行为树结构、执行语义、保存格式、自由拖动等现有功能，则停止该项。
4. 停止时把代码恢复到该任务开始前最后一个通过测试的状态，只提交测试证据、现状、尝试过程和阻塞原因。
5. 出现无法隔离的崩溃、资源损坏、非法内存访问或连续两次完整回归失败时，提前停止该项。
6. 本轮不继续追求布局耗时；只有明显卡死或无法完成交互才视为正确性阻塞。
7. 每项完成或停止后单独提交 Git，再开始下一项。

## 任务状态

| 编号 | 项目任务 | 状态 | 尝试次数 | Git 提交 | 主要证据 |
| --- | --- | --- | ---: | --- | --- |
| PRJ-P0-01 | 复现并界定树形布局问题 | 已完成 | 0 | `7c6b828` | 缩放拖动基线、验收矩阵 |
| PRJ-P0-02 | 层级避让并保留自由拖动 | 已完成 | 1 | 本项提交 | 1311/1311 专项断言、250/250 编辑器断言、154/154 运行时断言 |
| PRJ-P0-03 | 连线可读性 | 未开始 | 0 |  |  |
| PRJ-P1-04 | 始终曲线连线实验 | 未开始 | 0 |  |  |
| PRJ-P1-05 | 半透明卡片实验 | 未开始 | 0 |  |  |
| PRJ-P0-06 | 完整回归验证 | 未开始 | 0 |  |  |
| PRJ-P1-07 | 可玩游戏与复杂 NPC 证据 | 未开始 | 0 |  |  |

---

## PRJ-P0-01：复现并界定树形布局问题

### 基线结果

运行：

```powershell
./Godot_v4.6-stable_win64_console.exe --headless --audio-driver Dummy --path ./testgame/testgame --script res://tests/run_zoom_drag_layout_tests.gd
```

结果：

- `BT_ZOOM_DRAG_LAYOUT_TEST_SUMMARY passed=1186 failed=0`
- 31、61、121、241、364 节点规模均覆盖。
- 覆盖最小缩放 `0.100`、普通缩放 `1.000`、最大缩放 `2.074`，以及语义缩放边界附近的 `0.619/0.620/0.621` 和 `0.879/0.880/0.881`。
- 覆盖完整卡片、紧凑卡片、语义缩放、拖动中避让、松开后状态、撤销/重做、保存/重载和全零坐标损坏资源恢复。
- 现有用例中没有再次出现“缩放后所有卡片叠在一起”。
- 241 节点可玩树在完整缩放扫描中保持零卡片重叠，树结构、执行顺序和保存坐标不变。

### 为后续修改固定的验收用例

| 用例 | 操作 | 必须满足的结果 |
| --- | --- | --- |
| L1 自由布局保持 | 加载无重叠自由布局并切换缩放/卡片密度 | 所有未碰撞卡片保持用户位置，不产生隐藏偏移 |
| L2 拖动中局部避让 | 把一个叶节点拖到相邻节点上 | 拖动过程中立即无重叠；拖动节点跟随指针；只移动碰撞闭包 |
| L3 父子层级 | 让父子或相邻层发生碰撞并触发避让 | 自动移动不能把子节点推到父节点上方；父卡片底部与子卡片顶部保留清楚间距 |
| L4 兄弟顺序 | 在同一父节点的子节点之间制造碰撞 | 左到右的兄弟顺序不改变，执行顺序不改变 |
| L5 多尺寸与多规模 | 在 31、61、121、241、364 节点树的最小/中间/最大缩放下切换完整、紧凑和语义缩放 | 零卡片重叠，层级正确，结果稳定 |
| L6 显式自动布局 | 对可变尺寸节点执行 Auto Arrange 和 Arrange for Overview | 每一深度位于上一深度下方，兄弟顺序稳定，同样输入结果确定 |
| L7 资源安全 | 完成拖动、撤销/重做、保存/重载 | 非拖动节点的资源坐标不被临时避让写回；结构、参数和执行语义不变 |
| L8 连线前置检查 | 在布局完成后检查每条父子连线 | 后续 PRJ-P0-03 中验证连线不穿过无关卡片 |

### 当前判断

原来的大规模重叠故障已经被现有实现和 1186 项断言覆盖，因此本项不重复修补已经通过的缩放逻辑。但代码检查确认了一个能够单独处理的层级缺口：当 5 个以上节点的保存坐标完全相同时，`_seed_identical_auto_spacing_groups` 会按普通网格临时展开；31 节点生成树中的 Root 和它的第一个子节点会落在同一行。它们不再重叠，但不满足“子节点位于父节点下方”。现有测试只检查这类资源恢复后是否重叠，没有检查层级，所以仍然可以得到 1186/1186。下一项将为该情况先加入失败断言，再做局部修复。

另一个已界定缺口是：连线穿过无关卡片的检测函数已经存在于视觉测试中，但尚未作为正式断言运行，将在 PRJ-P0-03 处理。

**状态：已完成。** 已得到可重复基线、测试边界和后续验收用例，没有修改行为树资源或运行语义。

---

## PRJ-P0-02：让自动避让保持行为树层级，同时保留自由拖动

### 失败复现

先把 L3、L4、L5 写成自动断言。修改实现前，31、61、121、241、364 节点的全零坐标树在普通卡片、紧凑卡片、语义概览和阈值缩放中共出现 57 项失败。失败集中在：

- 子节点没有位于父节点下方；
- 完整深度行没有与上一行分开；
- 屏幕上的兄弟顺序与资源执行顺序不一致。

这证明“零重叠”与“仍然是一棵可读的树”并不是同一件事。

### 第一次修复

- 只在多个节点完全重叠、已经不存在可保留的相对位置时，按照行为树深度分行临时展开。
- 每一行按照资源中的稳定顺序排列，卡片之间使用实际最大卡片尺寸加 24 像素间距。
- 普通、没有碰撞的自由布局仍保持原位，不会自动变成固定格式。
- 直接相连的父子卡片发生碰撞时，沿纵向按父上子下避让；不相关卡片仍按最小移动方向进行局部避让。
- 拖动节点继续固定在指针指定位置，其他节点只使用临时显示偏移，不写回资源。
- 完全相同坐标的兄弟节点增加稳定的资源顺序回退，避免执行顺序由不确定排序决定。
- 活动插件和可分发模板同步修改。

### 验证结果

- 缩放与拖动专项：`BT_ZOOM_DRAG_LAYOUT_TEST_SUMMARY passed=1311 failed=0`。
- 编辑器功能：`BT_EDITOR_TEST_SUMMARY passed=250 failed=0`。
- 运行时与资源：`BT_TEST_SUMMARY passed=154 failed=0`。
- 覆盖 31、61、121、241、364 节点。
- 覆盖最小、普通、最大和语义阈值缩放。
- 覆盖普通卡片、紧凑卡片、语义概览、父子直接碰撞、全零坐标恢复。
- 活动插件与模板的修改文件 SHA-256 一致。
- 结构、参数、执行顺序、非拖动节点资源坐标、撤销/重做和保存/重载保持不变。

**状态：已完成。尝试次数：1/3。** 没有触发停止边界。
