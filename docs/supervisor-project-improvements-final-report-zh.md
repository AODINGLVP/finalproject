# 导师提出的七项项目改进完成报告

完成日期：2026-08-26

## 总体结果

七项项目工作均已达到预先写明的验收边界，并且每项都单独提交到 Git。没有未完成或被判定阻塞的项目。所有修改都保留行为树结构、保存资源和从左到右的执行顺序；本轮没有继续优化布局耗时。

## 七项完成情况

| 项目 | 完成结果 | Git 提交 |
| --- | --- | --- |
| 1. 复现并界定树形布局问题 | 建立五种节点规模、多个缩放和显示状态的固定验收矩阵；确认原测试只检查零重叠，没有充分检查父子层级 | `7c6b828` |
| 2. 保持层级的自由拖动与自动避让 | 全零坐标树按拓扑深度临时展开；父子碰撞保持父上子下；普通自由布局不被强制排成固定格式；兄弟执行顺序不变 | `7d4eaf5` |
| 3. 连线可读性 | 新增默认开启的 `Edge Obstacle Avoidance`；只有正常路线穿过无关卡片时才绕行，端口、点击断线、撤销和重做继续工作 | `2a9441c` |
| 4. 始终曲线实验 | 新增默认关闭的 `Always Curved Edges (Experiment)`；固定 241 节点对照中曲线更短，但穿过一张卡片，因此不替代默认避让 | `0fbdce0` |
| 5. 半透明卡片实验 | 新增默认关闭的 `Translucent Cards (Experiment)`；只把卡片背景 alpha 乘以 0.72，文字、端口、边框和状态色保持不透明 | `862fb8e` |
| 6. 完整回归 | 九组自动化测试 2225/2225；真实 GPU、游戏冒烟和编辑器启动正常；行为树资源未改写；活动插件与分发模板同步 | `c93b7bc` |
| 7. 可玩游戏证据 | 五名有限敌人分别使用 31/61/121/241/364 节点树；玩家无限生命；游戏可完成并重置；补充实际 241 节点树和真实 Runner Live Debug 截图 | `9fe9494` |

## 怎样使用改进后的编辑器

1. 打开 `testgame/testgame/project.godot`，在 Godot 底部打开 `Behavior Tree` 面板。
2. 从树选择器中选择需要编辑的 `.tres` 资源。创建节点时，在画布空白处右键并选择节点类型；工具栏不再放置一排节点创建按钮。
3. 从父节点底部端口连接到子节点顶部端口。同一父节点下的子节点仍按照画布横坐标从左到右执行。
4. 直接拖动节点即可自由调整位置。拖动过程中和拖动结束后都会进行局部避让；非拖动节点使用临时显示偏移，不会因为避让而改写其保存位置。
5. 打开 `Display > Advanced Display`：
   - `Edge Obstacle Avoidance` 默认开启，建议保持开启。
   - `Always Curved Edges (Experiment)` 默认关闭，只用于比较曲线效果。
   - `Translucent Cards (Experiment)` 默认关闭，只用于观察后方连线；若文字与背景线条混淆应立即关闭。
6. 其他大型树工具仍可独立使用，包括 Compact、Semantic Zoom、Search、Subtree Focus、Collapse、Fisheye、Minimap、Fit、布局和运行时路径显示。

## 怎样运行可玩游戏

1. 运行 `res://scenes/test_game.tscn`。
2. 使用 `A/D` 移动、`W` 跳跃或向上攀爬、`S` 向下攀爬、`J` 或鼠标左键攻击、`Space` 冲刺、`C` 隐身、`T` 暂停/继续 AI、`R` 重置。
3. 玩家显示 `HP INFINITE`。依次击败五名敌人，敌人不会自动复活；击败第五名后出现胜利面板。
4. 要看真实 241 节点敌人的运行路径，在 `Behavior Tree` 面板选择 `res://behavior_trees/arena_tactician_241.tres`，然后在 `Debug` 中开启 `Live Debug` 与 `Active Path Highlight`。

完整游戏步骤和证据分别见：

- `testgame/testgame/COMPLEX_ARENA_GUIDE.md`
- `docs/supervisor-playable-game-evidence-zh.md`

## 验证摘要

完整回归的九组测试合计 2225/2225，包括运行时与资源、编辑器、五种规模缩放拖动、基础游戏、复杂场景、通关、可玩复杂树、真实 GPU 视觉和复杂显示树。第 7 项又针对最终游戏流程运行 315/315 条断言，并完成真实渲染冒烟。两组数字包含重复覆盖的游戏测试，因此不相加成一个“唯一断言总数”。

日志中没有脚本错误、非零退出、泄漏、原生崩溃或非法内存访问。运行时测试中唯一警告来自一个故意调用不存在 Actor 方法的错误路径用例，测试确认它会被安全处理。

## 停止边界和保留限制

- 第 3 项曾尝试把障碍搜索扩大到全画布和屏幕外所有卡片。紧凑 241 节点矩阵持续高负载超过五分钟，因此该候选方案按停止边界回退。最终保留的按需局部绕行通过验收，没有把这个失败实现留在项目中。
- “始终曲线”和“半透明卡片”是完成的探索性功能，不是已经证明更好用的默认功能。两者均可独立关闭并安全恢复原显示。
- 本轮没有未完成的正式项目，也没有需要用户手工补救的资源损坏。性能优化不属于本轮目标，未继续投入。

更详细的每项复现、尝试次数、测试结果和截图位置见 `docs/supervisor-project-improvement-execution-log-zh.md`。
