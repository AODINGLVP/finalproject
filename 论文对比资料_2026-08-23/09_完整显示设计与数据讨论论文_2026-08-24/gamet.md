# 项目研究重点

## 核心研究目标

本项目研究的重点是：当前实现的一系列大型行为树显示优化方案，能否让游戏开发者更方便地使用可视化行为树开发和调试游戏人工智能。

正式研究问题为：

> **RQ：在不同的行为树节点规模和物理屏幕尺寸下，本项目提出的可组合显示优化，相较于传统基线显示，能否改善大型行为树的可测显示与编辑交互表现，同时保持行为树结构和执行语义不变？**

项目首先实现一个能够实际驱动游戏敌人的完整 Godot 4.6 行为树插件，使实验对象不是只能展示节点的静态原型。复杂演示游戏中的敌人由 241 节点行为树控制，覆盖巡逻、索敌、追击、近战、远程攻击、跳跃、攀爬、越障、搜索、撤退和治疗等行为。完整运行时、黑板、Decorator、Live Debug、编辑功能和复杂游戏共同组成经过验证的实验平台；它们用于保证实验对象正确且具有游戏开发意义，不用于研究行为树基础功能本身是否帮助游戏开发。

## 研究对象

实验只比较以下六种条件。每次实验开始前先展开全部节点、清空搜索、取消聚焦、将缩放恢复到 100%，并关闭本实验涉及的显示优化，然后执行当前条件的操作。

1. **Baseline**：不打开主要显示优化，显示完整卡片和全部分支，作为共同参照。
2. **Compact Cards**：只开启 Compact。所有节点仍然显示，但卡片只保留类型标识和短标题。
3. **Optimized Overview**：同时开启 Compact 与 Semantic Zoom，将画布缩放到 50%，以低细节卡片查看全局结构。
4. **Optimized Search**：先进入 Optimized Overview，再搜索预先固定且唯一的节点标题。目标进入视口并保持显著，其他节点淡化。
5. **Subtree Focus**：开启 Compact，选择预先固定的分支作为 Focus Root，只显示该分支及必要上下文。
6. **Context Collapse**：开启 Compact，保留通向固定目标的路径，折叠路径外的指定子树；折叠根仍显示，其后代由摘要代替。

上述条件必须可以安全恢复到 Baseline。比较过程中不得改变行为树资源结构、节点保存位置、父子关系和同级从左到右的执行顺序。插件中的其他显示功能不属于本论文实验，也不参与结果比较。

## 实验变量

实验采用三个主要自变量：

1. **显示条件**：优化前的 Baseline 与一种或多种优化后的显示条件。
2. **行为树节点数量**：使用 31、61、121、241 和 364 节点的确定性行为树。
3. **物理屏幕尺寸**：在当前连接的三块不同物理尺寸屏幕上重复实验。屏幕尺寸是研究因素；分辨率、刷新率、系统缩放和连接方式只作为审计信息，不作为主要解释变量。

核心比较关系为：

> 屏幕尺寸 × 节点数量 × 显示条件（优化前/优化后）

## 评判方式

项目通过同一行为树、同一任务和同一实验流程下的前后配对数据评价显示优化。主要记录：

- 画布内可见节点数量和比例；
- 不同屏幕尺寸下画布能够容纳的节点比例；
- 节点卡片总面积和保留的信息字段；
- Search 淡化的非目标节点比例和目标是否进入视口；
- Focus、Collapse 后保留的上下文数量；

每个条件先预热，再进行多次重复测量。结果报告原始数据、中位数、四分位数、P95、前后差值或降低比例，并保留实验配置、截图和输入文件哈希，使后续接入新屏幕后能够追加实验而不覆盖旧数据。

## 论文中的结论边界

这些实验能够说明显示优化是否提高了界面信息密度、可见上下文和目标定位能力，以及是否在不同节点数量和物理屏幕尺寸下保持稳定。它们不能在没有真人参与者数据的情况下直接证明人的理解速度、错误率、认知负荷或长期开发效率得到提高。

因此论文应使用“改善了可测的显示与交互属性”“为更方便地使用大型行为树提供了客观支持”等表述，不能把面积降低或覆盖率提高直接写成同等比例的可读性提升。

## 不属于本论文重点的内容

行为树 Tick 成本、Actor 数量下的运行时扩展、CPU/GPU 资源消耗和拓扑缓存性能不作为本论文的研究问题、主要实验、核心贡献或结论。相关实现可以作为插件内部工程细节保留，但不参与本文对显示优化有效性的论证。
