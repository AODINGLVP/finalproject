\chapter{引言}
\label{ch:introduction}

\section{研究背景}

现代游戏智能体需要在巡逻、追击、攻击、撤退和恢复等行为之间做出选择。对于小型原型，把全部决策写在单一过程式脚本中是可行的；但随着状态和中断条件增加，控制流会越来越难以检查和修改。行为树（Behaviour Tree, BT）把决策逻辑表示为控制节点和叶节点组成的层级结构。复合节点决定如何选择子节点，条件和动作则把抽象决策结构连接到游戏状态与 Actor 代码。高层策略可以独立于移动、动画或战斗方法进行编辑，这是行为树在游戏开发中的主要价值之一\parencite{colledanchise2018bt}。

商业引擎已经证明可视化行为编写的实用性。Unreal Engine 将节点--连线树、Decorator、黑板和编辑器运行状态结合\parencite{unrealbt}。Godot 4.6 提供 \texttt{EditorPlugin}、\texttt{GraphEdit} 和 \texttt{GraphNode} 等通用扩展接口，但并没有规定完整的可视化行为树工作流\parencite{godoteditorplugin,godotgraphedit}。因此本项目同时面对工程问题和研究问题：插件首先必须能正确执行行为树并接入任意 Actor；在此基础上，随着节点和运行标注增加，图形表示还必须保持可检查性。

传统节点--连线树能够直接表达父子拓扑，但会随宽度和深度增长而迅速占用空间。行为树卡片还可能显示参数、描述、Decorator、黑板键和执行状态，比只显示标签的普通层级图更大。当数十或数百张卡片放入有限画布时，开发者必须频繁缩放、平移和搜索，当前执行路径也可能淹没在非活动分支中。这不只是美观问题，因为节点图正是设计者定位任务、理解顺序、修改条件和诊断失败的主要工具。

因此，本项目把一个功能完整的 Godot 行为树插件作为大型树显示研究的平台。在保持上下端口和“同级从左到右执行”的语义基础上，加入可独立开关的焦点+上下文、复杂度管理、导航和运行时解释技术。每项技术都能单独关闭，一方面防止视觉功能故障拖垮编辑器，另一方面保留可复现的基线比较条件。

\section{问题定义与研究空白}

树和图可视化领域已经提出许多方法。鱼眼和焦点+上下文视图会为焦点附近的元素分配更多空间\parencite{furnas1986fisheye,lamping1995hyperbolic}；概览+细节保留全局参考视图\parencite{cockburn2008review}；多列布局可以改善大扇出树的浏览\parencite{song2010largefanout}；增量展开和折叠可以降低图复杂度，同时尽量保持用户的心理地图\parencite{dogrusoz2018complexity,misue1995mentalmap}；大型分层图研究还表明，路径任务的效率取决于具体表示和任务\parencite{bae2011quilts}。

然而，这些研究没有直接回答游戏行为树编辑器应如何组合上述机制。行为树与文件目录等普通层级不同：同级顺序具有执行语义；叶节点会调用代码；Decorator 可能阻止分支运行；图的状态会在每个运行 Tick 中变化。一个可用的方案必须保持执行顺序，支持编辑与撤销，公开黑板状态，并在不向游戏画面加入调试覆盖层的前提下解释失败。现有研究可以指导单项设计，但仍需要一个集成、可执行且可复现评估的 Godot 实现。

\section{研究目标与问题}

本文目标是设计、实现并评估一个 Godot 4.6 可视化行为树插件，使其既能驱动真实 NPC，又能让游戏开发者更方便地在大型树中定位、理解和修改行为。

研究问题如下：

\begin{description}
    \item[RQ1：] Godot 编辑器插件能否提供控制非简单二维游戏智能体所需的核心编写、运行时、黑板、Decorator 和实时调试能力？
    \item[RQ2：] 可独立开关的显示技术会如何改变大型行为树的可测表示属性；这些变化能否提高游戏开发者在可玩 241 节点树中定位 Action、追踪活动路径和编辑 Decorator 的任务表现？
    \item[RQ3：] 在不改变执行结果、并能安全处理资源原地修改的前提下，拓扑缓存能否降低不同树规模和 Actor 数量下的 Tick 成本？
\end{description}

RQ2 包含仪器化和人体两个部分。几何、显著性与交互数据可以说明编辑器具体改变了什么；是否降低完成时间、错误、导航次数或认知负荷则必须由参与者数据验证。本文已经实现相应实验工具，但不会伪造参与者结果。因此仪器化部分已经得到回答，人的任务表现部分在完成数据采集前仍保持开放。

\section{研究目标与成功标准}

项目的具体目标包括：实现可序列化的树、节点、Decorator 和类型化黑板资源；实现 Root、Sequence、Selector、Reactive Selector、Random Selector、Parallel、Repeat、Wait、Action 和 Condition 的 \textsc{success}、\textsc{failure} 与 \textsc{running} 语义；允许可复用组件为 NPC 指定树和 Actor，并由叶节点调用 Actor 方法；提供创建、连接、断开、拖拽、类型化编辑、校验、保存、加载、撤销和重做；只在 Godot 编辑器中显示路径、节点状态、黑板和失败原因；为大型树显示方法提供独立持久化开关和安全复位路径；构造确定性大树与复杂二维场景；最后以可复现证据评价正确性、几何显示、视觉回归、大树拖拽交互和洁净安装，并实现平衡的开发者任务实验用于人体评价。

成功标准不仅是插件能够启动。每类运行节点需要聚焦语义测试，编辑操作必须通过保存/重载和历史测试，游戏场景必须展示索敌、追击、攻击、搜索、撤退与恢复，视觉功能必须在真实渲染器上检查，缓存与非缓存执行必须得到相同终态。即使断言计数通过，只要日志出现非零退出、脚本错误、泄漏、原生崩溃或非法内存访问，仍判定测试失败。

\section{主要贡献}

本文的主要贡献为：
\begin{itemize}
    \item 一个可分发的 Godot 4.6 行为树插件，包含可执行复合节点、叶节点、Decorator、Actor 方法集成和类型化黑板；
    \item 一个仅位于编辑器中的实时调试桥，可报告活动路径、黑板值和失败原因，而不把调试 UI 耦合到游戏；
    \item 一组集成的 19 项可独立开关显示功能，覆盖紧凑/语义表示、焦点+上下文、子树裁剪、导航、路径强调、无障碍编码和连线显示；
    \item 一套可复现评估材料，包括 31、121、364 节点资源、可玩的 241 节点树、真实 GPU 截图、180 条重复显示观测、公式工作簿和 12 人/216 次的开发者实验协议；
    \item 一种精确拓扑签名缓存，可检测相同节点数量下的换序、改父级、Decorator 改归属和资源实例替换，并包含九组规模/负载测量。
\end{itemize}

\section{论文结构}

第\ref{ch:literature}章批判性回顾行为树和可扩展层级可视化；第\ref{ch:methodology}章定义设计科学与实验方法；第\ref{ch:method}章说明资源、运行时、编辑器、显示机制、游戏场景和测试架构；第\ref{ch:results}章报告功能、几何、视觉与运行时结果；第\ref{ch:discussion}章结合研究问题和文献讨论结果与局限；第\ref{ch:conclusion}章总结贡献和未来工作。附录提供功能、证据与复现实验索引。
